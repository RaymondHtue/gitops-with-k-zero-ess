apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: nginx-http
spec:
  hostnames:
    - "nginx.example.com"
  parentRefs:
  - name: shared-gateway
    namespace: nginx-gateway
    sectionName: http
  rules:
    - backendRefs:
        - name: nginx
          port: 80
